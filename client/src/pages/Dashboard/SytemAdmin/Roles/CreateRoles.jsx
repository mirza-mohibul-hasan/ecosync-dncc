import useTitle from "../../../../hooks/useTitle";

const CreateRoles = () => {
  useTitle("Create Role");
  return (
    <div>
      <h1>Create User Roles is Not Currently avilable</h1>
    </div>
  );
};

export default CreateRoles;
